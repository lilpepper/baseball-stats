# TODO

- Add a real data ingestion pipeline (FanGraphs/Baseball-Reference public exports).
- Swap the heuristic query parser with a real LLM endpoint and vector search.
- Expand the stat filters with postseason, park factors, and WAR ranges.
- Add player comparison view with radar charts.
- Provide export to CSV and shareable links for filtered views.
