# baseball-stats

Single-page historic baseball stats UI with sliders, dials, and a lightweight LLM-style query console.

## Run locally

From the repo root:

```bash
python -m http.server 8000
```

Then open `http://localhost:8000/index.html` in your browser.
