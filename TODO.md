# TODO

- Add a real data ingestion pipeline (FanGraphs/Baseball-Reference public exports) with update cadence.
- Swap the heuristic query parser with a real LLM endpoint, embeddings, and vector search.
- Expand the stat filters with postseason, park factors, WAR ranges, and platoon splits.
- Add player comparison view with radar charts and side-by-side trend lines.
- Provide export to CSV and shareable links for filtered views.
- Add accessibility passes: focus states, keyboard navigation, and ARIA labels for controls.
- Include saved filter presets for era-based exploration (Deadball, Expansion, Steroid, Statcast).
- Add data provenance badges per card with source links and last-updated timestamps.
- Improve loading state with skeleton cards and empty-state messaging.
- Add query history and prompt examples for faster LLM exploration.
